services:
  bank-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apex-bank-management
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - PORT=8080
